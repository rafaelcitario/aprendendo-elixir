IO.puts("
	10 / 2 = 5.0 		# O Resultado da divisão é sempre um float
	div(10, 2) = 5 		# Para obter um resultado inteiro utilizamos div
	rem(10, 2) = 0 		# Para obtermos o resto da divisão utilizamos rem
	rem(0x1F, 2) = 1 	# Também é possivel utilizar números hexadecimal, octal e binario
")

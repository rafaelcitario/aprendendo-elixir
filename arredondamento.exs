IO.puts("

3.4 -> round(3.4) = 3 # neste caso esta sendo arredondado para baixo, pois 4 é um número abaixo da média. 
3.6 -> round(3.6) = 4 # neste caso esta sendo arredondado para cima, pois 6 é um número acima da média.
3.4 -> trunc(3.4) = 3 # neste caso sempre sera arredondado para baixo utilizando trunc.
")
